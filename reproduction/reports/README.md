# 公开结果快照

这里保留小型指标文件、图表、日志和环境摘要，使 GitHub 上的报告链接可直接查看。数值与图表从原实验目录复制，SVG 仅清理行末空白；环境摘要省略了本机绝对路径，日志中的本机仓库路径替换为 `<repo>`。

- `rat-20260922-114432`：作者权重重评估。
- `train-gpu-20260922-194834`：CUDA 13 上的三种子从头训练。

原始数据、逐批采样、表征数组和模型检查点未纳入 Git；完整文件保存在本地 `reproduction/results/`，可根据上一级运行说明重新生成。各次运行的源码哈希记录对应当时执行的文件。
